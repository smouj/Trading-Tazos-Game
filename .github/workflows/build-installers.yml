name: Build Desktop Installers

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version (e.g. 0.3.0)'
        required: true
        default: '0.3.0'

permissions:
  contents: write

jobs:
  # ─── Linux (AppImage + .deb) ──────────────────────────
  build-linux:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Get version
        id: version
        run: echo "value=$(node -p "require('./package.json').version")" >> $GITHUB_OUTPUT

      - name: Build Linux installers
        run: bun run electron:build:linux -- --publish never -c.extraMetadata.version=${{ steps.version.outputs.value }}

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: linux-installers
          path: |
            dist-electron/*.AppImage
            dist-electron/*.deb
          retention-days: 30

  # ─── Windows (.exe NSIS + portable) ────────────────────
  build-windows:
    runs-on: windows-2022
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Get version
        id: version
        run: echo "value=$(node -p "require('./package.json').version")" >> $env:GITHUB_OUTPUT

      - name: Build Windows installers
        run: bun run electron:build:win -- --publish never -c.extraMetadata.version=${{ steps.version.outputs.value }}

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: windows-installers
          path: |
            dist-electron/*.exe
          retention-days: 30

  # ─── macOS (.dmg + .zip) ──────────────────────────────
  build-macos:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Get version
        id: version
        run: echo "value=$(node -p "require('./package.json').version")" >> $GITHUB_OUTPUT

      - name: Build macOS installers
        run: |
          bun run electron:build:mac -- --publish never -c.extraMetadata.version=${{ steps.version.outputs.value }} || {
            echo "First attempt failed (likely hdiutil race), retrying in 30s..."
            sleep 30
            bun run electron:build:mac -- --publish never -c.extraMetadata.version=${{ steps.version.outputs.value }}
          }

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: mac-installers
          path: |
            dist-electron/*.dmg
            dist-electron/*.zip
          retention-days: 30

  # ─── GitHub Release ────────────────────────────────────
  release:
    needs: [build-linux, build-windows, build-macos]
    runs-on: ubuntu-24.04
    if: startsWith(github.ref, 'refs/tags/v')
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: dist-electron

      - name: Display structure
        run: find dist-electron -type f

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          draft: true
          generate_release_notes: true
          files: |
            dist-electron/linux-installers/*
            dist-electron/windows-installers/*
            dist-electron/mac-installers/*
