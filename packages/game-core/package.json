{
  "name": "@ttg/game-core",
  "version": "0.11.0",
  "private": true,
  "description": "TTG game engine — pure rules, types, and constants. Zero dependencies.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "files": ["src"],
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  }
}
